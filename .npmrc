# CI runs `pnpm install --frozen-lockfile` explicitly before every build;
# skip pnpm's redundant pre-run install check (it can be flaky and slows
# every `pnpm run` invocation).
verify-deps-before-run=false
